'use strict';
const assert = require('assert');
const LinerTransform = require('../components/AggregatorTransform');
const fs = require('fs');

describe('Aggregator', () => {
	it('the total size of the packages should equal the size of the original file', ( done ) => {

	});
});